#!/usr/bin/env node

/**
 * This file provides a cli helpline for developers to quickly check
 * the output of different commands without touching the file-system.
 *
 * Check the source code for more documentation
 */
"use strict";

var _commander = _interopRequireDefault(require("commander"));

var _process = _interopRequireDefault(require("process"));

var _Bootstrap = require("../scripts/Bootstrap");

var _utils = require("./utils");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

_commander.default.command('bootstrap').description('Check the CLI output of bootstrap command').action(async () => {
  const bs = new _Bootstrap.Bootstrap(_process.default.cwd(), '0.0.0-dev');
  const answer = await bs.getUserInput();
  console.log(JSON.stringify(answer, null, 4));
});

_commander.default.command('intro').description('Check CLI intro').action(() => {
  (0, _utils.printIntro)();
});

_commander.default.parse(_process.default.argv);