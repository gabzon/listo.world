"use strict";

var _helperPluginUtils = require("@babel/helper-plugin-utils");

var _preset = require("./preset");

/**
 * Export a function to declare this preset.
 *
 * It takes options from userland and passes to @babel/preset-env
 */
module.exports = (0, _helperPluginUtils.declare)((api, opts) => {
  // Check if version is 7
  api.assertVersion(7); // Give back the babel config based on options

  return (0, _preset.preset)(opts);
});